#include "main.h"

int main() {
    int returnValue = compile();

    return returnValue;
}


/*
 * f = forward
 * l = left
 * r = right
 * L = laser
 * 0 = <none>    the runtime should ignore characters that are zeros
 * */
